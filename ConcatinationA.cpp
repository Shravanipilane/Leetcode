class solution
{
    public:
        vector<int> getConcatenation(vector<int>& nums)
        {
            int sz = nums.Size();
            vector<int>ans(2 * sz);

            for(i=0;i<sz;i++)
            {
                ans[i] = nums[i];
                ans[i + sz] = nums[i];
            } 
            return ans;

        }
};

vector<int> getConcatenation(vector<int>& nums)
{
    int n = nums.Size();
    vector<int>ans(2 * sz);

    for(int i =0;i<sz;i++)
    {
        ans[i] = nums[i];
        ans[i + sz] = nums[i];
    }
    return ans;
} 

vector<int> getConcatenation(vector<int>& nums)
{
    int n = nums.Size();
    vector<int>ans(2 * sz);

    for(int i=0;i<sz;i++)
    {
        ans[i] = nums[i];
        ans[i + sz] = nums[i];
    }
    return ans;
}


vector<int> getConcatenation(vector<int>& nums)
{
    int n = nums.Size();
    vector<int>ans(2 * sz);

    for(int i=0;i<sz;i++)
    {
        ans[i] = nums[i];
        ans[i + sz] = nums[i];
    }
    return ans
}

int n = nums.Size();
vector<int>ans(2 * sz);
for(i=0;i<sz;i++)
{
    ans[i] =  nums[i];
    ans[i + sz] = nums[i];
}
return ans;



int sz = nums.Size();
vector<int>ans(2 * sz);
for(int i=0;i<sz;i++)
{
    ans[i] = nums[i];
    ans[i + sz] = nums[i];
}
return ans;


int sz = nums.size();
vector<int>ans(2 * sz);
for(i=0;i<n;i++)
{
    ans[i] = nums[i];
    ans[i + sz] = nums[i];
}


vector<int>ans(2 * sz);
for(i=0;i<n;i++)
{
    ans[i] = nums[i];
    ans[i + sz] = nums[i];
}
